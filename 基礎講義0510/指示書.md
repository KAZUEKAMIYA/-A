# 指示書

最終更新: 2026-05-10 セッション1（終了: 2026-05-11）

---

## 1. 最初に読むべきファイル

1. `C:\Users\kazuk\OneDrive\デスクトップ\基礎講義0510\ログ\2026-05-10_セッション1ログ.md` — 前セッション作業記録
2. `C:\Users\kazuk\.claude\skills\chat-summary\SKILL.md` — 未完了タスク対象スキル

---

## 2. 全セッション累計の完了事項

| # | セッション | 内容 | コミット |
|---|-----------|------|---------|
| 1 | 基礎講義0503 セッション1 | `skill-creator` ジャンクション作成（スラッシュコマンド登録） | N/A（git管理外） |
| 2 | 基礎講義0503 セッション2 | `chat-summary`スキル新規作成・Playwright画像生成動作確認済み | N/A（git管理外） |
| 3 | 基礎講義0510 セッション1 | /session-start で前回引き継ぎ把握のみ（作業なし） | N/A（git管理外） |

---

## 3. 未完了タスク

1. **YouTube講義の文字起こし受け取り・処理**（優先度：高）
   - ユーザーがYouTubeライブ配信終了後に文字起こしを投げる予定
   - 受け取ったらこのディレクトリ（`基礎講義0510/`）に保存・処理する

2. **SKILL.mdのNode.js対応更新**（優先度：中）
   - ファイル: `C:\Users\kazuk\.claude\skills\chat-summary\SKILL.md`
   - 内容: スクリプト実行コマンドがPython版のまま
   - 修正: `generate_images.py` → `generate_images.js`、`python` → `node`

3. **session-startでこの指示書を使う**（優先度：低）
   - 次回 `/session-start` 時にはこのファイル（`基礎講義0510/指示書.md`）を選ぶ

---

## 4. 品質ゲートフロー

このプロジェクトは現在 git 管理外・テスト未設定。
スキル登録後の動作確認は Claude Code 再起動 → スキル名を呼び出して行う。

---

## 5. やってはいけないことリスト

- `chat-summary`スキルでPythonスクリプト（generate_images.py）を実行しない（Pythonが未インストール）
- `~/.claude/skills/` への直接ファイルコピーではなくジャンクションを使う（他スキルと整合性を保つため）
- Claude Code を再起動しないままスキル登録効果を期待しない（スキルは起動時に読み込まれる）

---

## 6. 現在のgit状態

- リポジトリ: git管理外

---

## 7. 重要な数値（全て実測値）

| 項目 | 値 |
|------|-----|
| chat-summary スキルパス | `C:\Users\kazuk\.claude\skills\chat-summary\` |
| 画像生成スクリプト | `scripts\generate_images.js` |
| Playwright版 | playwright@1.59.1 |
| Node.js版 | v24.14.1 |
| node_modules場所 | `C:\Users\kazuk\.claude\skills\chat-summary\scripts\node_modules\` |

---

## 8. このファイルの使い方

次セッション開始時に `/session-start` を実行する。
探索候補として `基礎講義0510/指示書.md` が自動選択される。
作業終了時は `/session-end` を実行してこのファイルと `ログ/` を更新する。
