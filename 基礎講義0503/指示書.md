# 指示書

最終更新: 2026-05-03 セッション2

---

## 1. 最初に読むべきファイル

1. `C:\Users\kazuk\OneDrive\デスクトップ\新しいフォルダー\ログ\2026-05-03_セッション2ログ.md` — 前セッション作業記録
2. `C:\Users\kazuk\.claude\skills\chat-summary\SKILL.md` — 今セッションで作成したスキル

---

## 2. 全セッション累計の完了事項

| # | 内容 | コミット |
|---|------|---------|
| 1 | `~/.claude/skills/skill-creator` ジャンクション作成（スラッシュコマンド登録） | N/A（git管理外） |
| 2 | `chat-summary`スキル新規作成（SKILL.md + generate_images.js）、Playwrightで画像生成動作確認済み | N/A（git管理外） |

---

## 3. 未完了タスク

1. **SKILL.mdのNode.js対応更新**（優先度：中）
   - ファイル: `C:\Users\kazuk\.claude\skills\chat-summary\SKILL.md`
   - 内容: スクリプト実行コマンドがPython版のまま。`generate_images.py` → `generate_images.js`、`python` → `node` に修正が必要

2. **session-startの指示書選択**（優先度：低）
   - 次回session-start時に以下から選択する：
     1. `C:/Users/kazuk/OneDrive/デスクトップ/KDP/指示書.md`
     2. `C:/Users/kazuk/OneDrive/デスクトップ/指示書.md`（← このファイル）

---

## 4. 品質ゲートフロー

このプロジェクトは現在 git 管理外・テスト未設定。
スキル登録後の動作確認は Claude Code 再起動 → スキル名を呼び出して行う。

---

## 5. やってはいけないことリスト

- `chat-summary`スキルでPythonスクリプト（generate_images.py）を実行しない（Pythonが未インストール）
- `~/.claude/skills/` への直接ファイルコピーではなくジャンクションを使う（他スキルと整合性を保つため）
- Claude Code を再起動しないままスキル登録効果を期待しない（スキルは起動時に読み込まれる）

---

## 6. 現在の git 状態

- リポジトリ: git管理外

---

## 7. 重要な数値（全て実測値）

| 項目 | 値 |
|------|-----|
| chat-summary スキルパス | `C:\Users\kazuk\.claude\skills\chat-summary\` |
| 画像生成スクリプト | `scripts\generate_images.js` |
| Playwright版 | playwright@1.59.1 |
| Node.js版 | v24.14.1 |
| node_modules場所 | `C:\Users\kazuk\.claude\skills\chat-summary\scripts\node_modules\` |

---

## 8. このファイルの使い方

次セッション開始時に `/session-start` を実行するか、このファイルを読んで前回の状態を把握してから作業を開始してください。

作業終了時は `/session-end` を実行してこのファイルと `ログ/` を更新します。
